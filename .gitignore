# build artifacts
node_modules
dist
.ssr
*.tsbuildinfo
.wrangler/

# ---------------------------------------------------------------------------
# Build-time derived verification data.
# Everything below is regenerated by `node scripts/fetch-verified.mjs` (wired to
# `predev` / `prebuild`) from the single active backend (VERIFIED_BACKEND=dir|r2).
# Committing it would create a second website source of truth, which
# docs/repo-structure.md §1/§4.2.1 and deployment-contract.md §1 forbid.
# ---------------------------------------------------------------------------
/data/
/public/screenshots/
/src/data/generated.json

# NOTE: `fixtures-demo/` is *temporary* demo input for the bootstrap (dir) backend.
# It must be deleted once Repo C produces real fixtures.
