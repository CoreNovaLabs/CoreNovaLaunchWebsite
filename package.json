{
  "name": "corenovalaunch-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CoreNova Launch official website (Repo A) - Vite + React, renders build-time verified data from Repo C (dir fixtures) or Cloudflare R2",
  "scripts": {
    "predev": "node scripts/fetch-verified.mjs --allow-stale",
    "dev": "vite",
    "fetch-data": "node scripts/fetch-verified.mjs",
    "prebuild": "node scripts/fetch-verified.mjs",
    "build": "tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir .ssr && node scripts/prerender.mjs",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "react-markdown": "^9.0.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.6"
  }
}
